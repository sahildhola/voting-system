{
  "name": "votechain",
  "version": "1.0.0",
  "description": "Blockchain Voting System — Hardhat + Express + React + MongoDB Atlas",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix blockchain && npm install --prefix backend && npm install --prefix frontend",
    "chain": "npm run node --prefix blockchain",
    "deploy": "npm run deploy --prefix blockchain",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm start --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "test:contract": "npm test --prefix blockchain"
  }
}
